# Clean slate (2026-07-10)

User request: puri zip clean — naya build baad me instructions se.

## Kya hataaya

- `releases/*.zip` — saari purani versions (~80+ files)
- Root pe `hivirtus-*.zip`
- `build/`, `dist/` build cache

## Kya rakha

- `module/` — source code (JNI, scripts, UI, config)
- `docs/` — phase guides
- `overlay-app/` — reference only (zip me nahi jaata)

## Next

User batayega requirements → tab nayi clean zip build hogi.
